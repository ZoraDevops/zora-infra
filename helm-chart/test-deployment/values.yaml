namespace: zora-dev

replicaCount: 1

image:
  repository: http://192.168.40.130:8081/repository/docker-test/zora-test
  pullPolicy: IfNotPresent
  tag: "1.0.0"

service:
  type: ClusterIP
  port: 80        # Exposed port for ingress
  targetPort: 8080  # Actual app port inside the container

ingress:
  enabled: true
  className: "nginx"
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  hosts:
    - host: customers.zora.local
      paths:
        - path: /
          pathType: Prefix
  tls: []  # No TLS for local dev

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
